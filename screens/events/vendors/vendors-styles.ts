import { StyleSheet } from "react-native";

const size = 50


export const VendorStyles = StyleSheet.create({
    unselected: {
        height: size,
        width: size,
        borderRadius: size/2
    }
})